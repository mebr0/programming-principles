#include<iostream>
#include<cmath>
using namespace std;
int main()
{
	double x, y, z, a, b, c;
    cin>>x>>y>>z;
    a=(1+pow(sin(x+y),2))/(2+abs(x-2*x/abs(1+x*x*y*y)))+x;
    b=pow(cos(atan(1/z)), 2);
    cout<<a<<" "<<b;
    
}

